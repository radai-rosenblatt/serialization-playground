pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "serialization-playground"

//modules
include "avro-models"
include "proto-models"